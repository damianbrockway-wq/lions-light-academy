# teacher_id.html is deliberately not in this repo

The teacher ID card template embeds photographs of real people as base64 — it is
9.4 MB and it is pictures of your teachers. **Anything in this repository is
published publicly** the moment it is pushed, so it is kept out on purpose.

It lives in the local build kit tarball instead:
`~/Documents/Claude/Projects/lla/lla-build-kit-<date>.tar.gz`

`build_ids_scaled.py` and `stamp_id_cutlines.py` are here and will work as soon
as `teacher_id.html` is put beside them from the tarball.
